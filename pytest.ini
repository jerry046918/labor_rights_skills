[pytest]
markers =
    integration: marks tests as integration tests (require FunASR installed; slow)
    slow: marks tests as slow
addopts = -m "not integration and not slow"
testpaths = scripts/tests tests
